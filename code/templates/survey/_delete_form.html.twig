<form method="post" class="d-inline-block"  action="{{ path('app_survey_delete', {'id': survey.id}) }}" onsubmit="return confirm('Are you sure you want to delete this survey?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ survey.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
