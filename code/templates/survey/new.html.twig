{% extends 'base.html.twig' %}

{% block title %}New Survey{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mt-5 mb-3">Create new Survey</h1>

        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-primary" href="{{ path('app_survey_index') }}">Back to Surveys</a>
            </div>

            <div class="card-body">
                {{ include('survey/_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
